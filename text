<?php
    $server = 'localhost';
    $db = 'dontskip_main';
    $user = 'dontskip_alex';
    $pass = 'Wel#3Alex';
    $con = new mysqli($server,$user,$pass,$db);
    if($con->connect_error)
    {
        die("Connection Failed".$con->connect_error);
    }
    else
    {
        echo "connectedd";
    }
?>